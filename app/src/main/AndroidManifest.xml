<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.tallertabletlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListarCel" />
        <activity android:name=".Celular" />
        <activity android:name=".crearCel" />
        <activity android:name=".Repot2" />
        <activity android:name=".Repot3" />
        <activity android:name=".Repot4" />
        <activity android:name=".RepotTotal"></activity>
    </application>

</manifest>